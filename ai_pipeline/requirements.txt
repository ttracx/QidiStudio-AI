# AI Photo-to-3D Pipeline Server requirements
# Python 3.10/3.11 recommended

# Web framework
flask==3.1.0
Werkzeug==3.1.3

# Image processing
Pillow>=10.2.0
rembg>=2.0.50          # background removal
numpy>=1.24.0,<2.0

# TRELLIS (primary mesh generator)
# Install from source: pip install extensions/nvdiffrast extensions/diff-gaussian-rasterization extensions/simple-knn
# Clone: git clone https://github.com/microsoft/TRELLIS.git
torch>=2.1.0
torchvision>=0.16.0
einops
transformers>=4.40.0
diffusers>=0.27.0
imageio
imageio-ffmpeg
trimesh>=4.0.0
pymeshlab>=2023.12    # mesh repair/cleanup
open3d>=0.18.0        # mesh processing
scipy
easydict
omegaconf
huggingface-hub>=0.23.0
plyfile
tqdm

# TripoSR (fallback fast path)
# Clone: git clone https://github.com/VAST-AI-Research/TripoSR.git
# pip install -r TripoSR/requirements.txt

# Misc
gunicorn>=21.2.0     # production WSGI
python-dotenv>=1.0.0

# ---------------------------------------------------------------------------
# THOX printer-agent layer (ai_pipeline/thox/)
# ---------------------------------------------------------------------------
# Scan-to-print and live print-health monitoring. Deliberately light: this set
# alone is enough to run `python -m thox.app`, so print monitoring can live on a
# small always-on box next to the printer without a CUDA stack. numpy, Pillow
# and flask are already required above.
requests>=2.31         # Moonraker + vision provider HTTP
